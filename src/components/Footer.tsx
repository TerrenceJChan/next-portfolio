const Footer = () => {
  return (
    <footer className="flex h-16 items-center justify-center">
      <p>© {new Date().getFullYear()} Terrence Chan</p>
    </footer>
  );
};

export default Footer;
